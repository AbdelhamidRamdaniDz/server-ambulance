<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>جدول المناوبات - <%= user.name %></title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    
    <style>
        :root {
            --primary-color: #1976d2;
            --success-color: #28a745;
            --warning-color: #ff9800;
            --danger-color: #f44336;
            --light-bg: #f8f9fa;
            --card-bg: #ffffff;
            --text-color: #2c3e50;
            --text-secondary: #6c757d;
            --border-color: #e9ecef;
            --card-shadow: 0 5px 25px rgba(0,0,0,0.07);
            --radius: 16px;
        }
        body { 
            font-family: 'Cairo', sans-serif; 
            background-color: var(--light-bg); 
            margin: 0; 
            padding: 20px; 
        }
        .container { 
            max-width: 1200px; 
            margin: 20px auto; 
        }
        .header { 
            text-align: center; 
            margin-bottom: 40px; 
        }
        .header h1 { 
            font-size: 2.8rem; 
            color: var(--text-color); 
        }
        .header p { 
            font-size: 1.2rem; 
            color: var(--text-secondary); 
        }
        .stats-container { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); 
            gap: 25px; 
            margin-bottom: 40px; 
        }
        .stat-card { 
            background: var(--card-bg); 
            border-radius: var(--radius); 
            padding: 25px; 
            box-shadow: var(--card-shadow); 
            display: flex; 
            align-items: center; 
        }
        .stat-icon-container { 
            width: 60px; 
            height: 60px; 
            border-radius: var(--radius); 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            margin-left: 20px; 
        }
        .stat-text-container { 
            text-align: right; 
        }
        .stat-number { 
            font-size: 2rem; 
            font-weight: 700; 
            color: var(--text-color); 
        }
        .stat-label { 
            font-size: 1rem; 
            color: var(--text-secondary); 
        }
        .departments-container { 
            display: grid; 
            grid-template-columns: 1fr; 
            gap: 30px; 
        }
        .department-card { 
            background: var(--card-bg); 
            border-radius: var(--radius); 
            box-shadow: var(--card-shadow); 
            overflow: hidden; 
            border-left: 5px solid #ccc; /* fallback, will be overridden inline */
        }
        .card-header { 
            padding: 20px; 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
        }
        .header-right { 
            display: flex; 
            align-items: center; 
        }
        .department-icon { 
            font-size: 24px; 
            color: white; 
            width: 44px; 
            height: 44px; 
            border-radius: 12px; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            margin-left: 15px; 
        }
        .department-title { 
            font-size: 1.6rem; 
            font-weight: 700; 
            color: var(--text-color); 
        }
        .staff-count { 
            font-size: 1rem; 
            font-weight: 600; 
            color: var(--text-secondary); 
        }
        .progress-container { 
            padding: 0 20px 20px; 
        }
        .progress-bar { 
            height: 8px; 
            background-color: var(--border-color); 
            border-radius: 4px; 
            overflow: hidden; 
            margin-top: 10px; 
        }
        .progress-fill { 
            height: 100%; 
            border-radius: 4px; 
        }
        .staff-container { 
            padding: 0 20px 20px; 
        }
        .staff-item { 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            padding: 15px 0; 
            border-bottom: 1px solid var(--border-color); 
        }
        .staff-item:last-child { 
            border-bottom: none; 
        }
        .staff-details { 
            text-align: right; 
            flex: 1; 
            margin-right: 15px; 
        }
        .staff-name { 
            font-size: 1.1rem; 
            font-weight: 600; 
            color: var(--text-color); 
        }
        .staff-role { 
            font-size: 0.9rem; 
            color: var(--text-secondary); 
        }
        .status-badge { 
            display: flex; 
            align-items: center; 
            padding: 6px 12px; 
            border-radius: 20px; 
            font-size: 0.9em; 
            font-weight: 600; 
        }
        .status-dot { 
            width: 8px; 
            height: 8px; 
            border-radius: 50%; 
            margin-left: 8px; 
        }
        .back-link { 
            display: inline-block; 
            margin-top: 30px; 
            color: var(--primary-color); 
            text-decoration: none; 
            font-weight: 700; 
        }
        .no-data { 
            text-align: center; 
            color: var(--text-secondary); 
            padding: 40px; 
            font-size: 1.2rem; 
        }
    </style>
</head>
<body>
    <%
        const getStatusColor = (status) => {
            switch (status) {
                case 'available': return '#28a745';
                case 'limited': return '#ff9800';
                case 'unavailable': return '#f44336';
                default: return '#6c757d';
            }
        };
        const getStatusText = (status) => {
            switch (status) {
                case 'available': return 'متاح';
                case 'limited': return 'محدود';
                case 'unavailable': return 'غير متاح';
                default: return 'غير محدد';
            }
        };
        // Helper to add alpha to hex color (for status badge background)
        function hexToRgba(hex, alpha) {
            hex = hex.replace('#', '');
            if (hex.length === 3) {
                hex = hex.split('').map(x => x + x).join('');
            }
            const num = parseInt(hex, 16);
            const r = (num >> 16) & 255;
            const g = (num >> 8) & 255;
            const b = num & 255;
            return `rgba(${r},${g},${b},${alpha})`;
        }
    %>
    <div class="container">
        <div class="header">
            <h1>جدول المناوبات</h1>
            <p>نظرة عامة على حالة الأقسام والطواقم الطبية في المستشفى</p>
        </div>

        <div class="stats-container">
            <div class="stat-card">
                <div class="stat-icon-container" style="background-color: #e3f2fd;"><i class="fas fa-users" style="color: #2196f3;"></i></div>
                <div class="stat-text-container">
                    <div class="stat-number"><%= totalActiveStaff %></div>
                    <div class="stat-label">طاقم نشط</div>
                </div>
            </div>
            <div class="stat-card">
                <div class="stat-icon-container" style="background-color: #e8f5e9;"><i class="fas fa-building-user" style="color: #4caf50;"></i></div>
                <div class="stat-text-container"><div class="stat-number"><%= departments.length %></div><div class="stat-label">قسم نشط</div></div>
            </div>
            <div class="stat-card">
                <div class="stat-icon-container" style="background-color: #fff3e0;"><i class="fas fa-chart-pie" style="color: #ff9800;"></i></div>
                <div class="stat-text-container"><div class="stat-number"><%= availabilityPercentage %>%</div><div class="stat-label">نسبة التوفر</div></div>
            </div>
        </div>

        <div class="departments-container">
            <% if (departments.length > 0) { %>
                <% departments.forEach(dept => { %>
                    <% const availability = (dept.activeStaffCount / dept.staff.length) * 100 || 0; %>
                    <div class="department-card" style="border-left: 5px solid <%= dept.color || '#ccc' %>;">
                        <div class="card-header">
                            <span class="staff-count"><%= dept.activeStaffCount %> / <%= dept.staff.length %> نشط</span>
                            <div class="header-right">
                                <span class="department-icon" style="background-color: <%= dept.color || '#ccc' %>;"><i class="fas fa-<%= dept.icon %>"></i></span>
                                <h2 class="department-title"><%= dept.name %></h2>
                            </div>
                        </div>
                        <div class="progress-container">
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: <%= availability %>%; background-color: <%= dept.color || '#ccc' %>;"></div>
                            </div>
                        </div>
                        <div class="staff-container">
                            <% dept.staff.forEach(member => { %>
                                <div class="staff-item">
                                    <%
                                        // Use RGBA for background with alpha for badge
                                        const badgeBg = hexToRgba(getStatusColor(member.status), 0.12);
                                        const badgeColor = getStatusColor(member.status);
                                    %>
                                    <div class="status-badge" style="background-color: <%= badgeBg %>; color: <%= badgeColor %>;">
                                        <span class="status-dot" style="background-color: <%= badgeColor %>;"></span>
                                        <%= getStatusText(member.status) %>
                                    </div>
                                    <div class="staff-details">
                                        <div class="staff-name"><%= member.doctor.firstName %> <%= member.doctor.lastName %></div>
                                        <div class="staff-role"><%= member.roleInDepartment %></div>
                                    </div>
                                </div>
                            <% }); %>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p class="no-data">لا توجد أقسام معرفة لهذا المستشفى بعد.</p>
            <% } %>
        </div>

         <a href="/hospital-panel/dashboard" class="back-link">
            <i class="fas fa-arrow-right" style="margin-left: 8px;"></i>
            العودة إلى لوحة التحكم
        </a>
    </div>
</body>
</html>
