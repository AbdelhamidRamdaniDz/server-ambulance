<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>جدول المناوبات - <%= user.displayName %></title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    
    <style>
        :root {
            --primary-color: #1976d2;
            --success-color: #28a745;
            --warning-color: #ff9800;
            --danger-color: #f44336;
            --light-bg: #f8f9fa;
            --card-bg: #ffffff;
            --text-color: #2c3e50;
            --text-secondary: #6c757d;
            --border-color: #e9ecef;
            --card-shadow: 0 5px 25px rgba(0,0,0,0.07);
            --radius: 16px;
        }
        body { 
            font-family: 'Cairo', sans-serif; 
            background-color: var(--light-bg); 
            margin: 0; 
            padding: 20px; 
        }
        .container { 
            max-width: 1200px; 
            margin: 20px auto; 
        }
        .header { 
            text-align: center; 
            margin-bottom: 40px; 
        }
        .header h1 { 
            font-size: 2.8rem; 
            color: var(--text-color); 
        }
        .header p { 
            font-size: 1.2rem; 
            color: var(--text-secondary); 
        }
        .stats-container { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); 
            gap: 25px; 
            margin-bottom: 40px; 
        }
        .stat-card { 
            background: var(--card-bg); 
            border-radius: var(--radius); 
            padding: 25px; 
            box-shadow: var(--card-shadow); 
            display: flex; 
            align-items: center; 
        }
        .stat-icon-container { 
            width: 60px; 
            height: 60px; 
            border-radius: var(--radius); 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            margin-left: 20px; 
        }
        .stat-text-container { 
            text-align: right; 
            flex: 1;
        }
        .stat-number { 
            font-size: 2rem; 
            font-weight: 700; 
            color: var(--text-color); 
        }
        .stat-label { 
            font-size: 1rem; 
            color: var(--text-secondary); 
        }
        .departments-container { 
            display: grid; 
            grid-template-columns: 1fr; 
            gap: 30px; 
        }
        .department-card { 
            background: var(--card-bg); 
            border-radius: var(--radius); 
            box-shadow: var(--card-shadow); 
            overflow: hidden; 
            border-right: 5px solid; 
        }
        .card-header { 
            padding: 20px; 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
        }
        .header-right { 
            display: flex; 
            align-items: center; 
        }
        .department-icon { 
            font-size: 24px; 
            color: white; 
            width: 44px; 
            height: 44px; 
            border-radius: 12px; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            margin-left: 15px; 
        }
        .department-title { 
            font-size: 1.6rem; 
            font-weight: 700; 
            color: var(--text-color); 
        }
        .availability-badge {
            font-size: 0.9rem;
            font-weight: 600;
            padding: 5px 12px;
            border-radius: 20px;
        }
        .progress-container { 
            padding: 0 20px 20px; 
        }
        .progress-label {
            display: flex;
            justify-content: space-between;
            font-size: 0.9rem;
            color: var(--text-secondary);
            margin-bottom: 8px;
        }
        .progress-bar { 
            height: 8px; 
            background-color: var(--border-color); 
            border-radius: 4px; 
            overflow: hidden; 
        }
        .progress-fill { 
            height: 100%; 
            border-radius: 4px; 
        }
        .staff-container { 
            padding: 10px 20px 20px; 
            border-top: 1px solid var(--border-color);
            margin-top: 20px;
        }
         .staff-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--text-color);
            margin-bottom: 15px;
        }
        .staff-item { 
            display: flex; 
            align-items: center; 
            margin-bottom: 12px;
        }
        .staff-avatar {
            width: 40px;
            height: 40px;
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-left: 12px;
        }
        .staff-avatar i {
            color: white;
            font-size: 18px;
        }
        .staff-details { 
            text-align: right; 
            flex: 1; 
        }
        .staff-name { 
            font-size: 1.1rem; 
            font-weight: 600; 
            color: var(--text-color); 
        }
        .staff-role { 
            font-size: 0.9rem; 
            color: var(--text-secondary); 
        }
        .card-actions {
            background-color: var(--light-bg);
            padding: 15px 20px;
            display: flex;
            justify-content: flex-end;
            gap: 15px;
            border-top: 1px solid var(--border-color);
        }
        .action-btn {
            text-decoration: none;
            padding: 8px 16px;
            border-radius: 8px;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            border: 1px solid transparent;
        }
        .details-btn { background-color: var(--primary-color); color: white; }
        .edit-btn { background-color: var(--card-bg); color: var(--text-secondary); border-color: var(--border-color); }
        .back-link { 
            display: block; 
            margin-top: 30px;
            text-align: center;
            color: var(--primary-color); 
            text-decoration: none; 
            font-weight: 700; 
        }
        .no-data { 
            text-align: center; 
            color: var(--text-secondary); 
            padding: 40px; 
            font-size: 1.2rem; 
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>جدول المناوبات</h1>
            <p>نظرة عامة على حالة الأقسام والطواقم الطبية في المستشفى</p>
        </div>

        <div class="stats-container">
            <div class="stat-card">
                <div class="stat-icon-container" style="background-color: #e3f2fd;"><i class="fas fa-users" style="color: #2196f3;"></i></div>
                <div class="stat-text-container">
                    <div class="stat-number"><%= totalActiveStaff %></div>
                    <div class="stat-label">طاقم نشط</div>
                </div>
            </div>
            <div class="stat-card">
                <div class="stat-icon-container" style="background-color: #e8f5e9;"><i class="fas fa-building-user" style="color: #4caf50;"></i></div>
                <div class="stat-text-container"><div class="stat-number"><%= departments.length %></div><div class="stat-label">قسم نشط</div></div>
            </div>
            <div class="stat-card">
                <div class="stat-icon-container" style="background-color: #fff3e0;"><i class="fas fa-chart-pie" style="color: #ff9800;"></i></div>
                <div class="stat-text-container"><div class="stat-number"><%= availabilityPercentage %>%</div><div class="stat-label">نسبة التوفر</div></div>
            </div>
        </div>

        <div class="departments-container">
            <% if (departments.length > 0) { %>
                <% departments.forEach(dept => { %>
                    <% const availability = (dept.activeStaffCount / dept.staff.length) * 100 || 0; %>
                    <% const onDutyStaff = dept.staff.filter(s => s.onDuty); %>
                    <div class="department-card" style="border-right-color: <%= dept.isAvailable ? 'var(--success-color)' : 'var(--danger-color)' %>;">
                        <div class="card-header">
                            <span class="availability-badge" style="background-color: <%= dept.isAvailable ? 'var(--success-color)' : 'var(--danger-color)' %>20; color: <%= dept.isAvailable ? 'var(--success-color)' : 'var(--danger-color)' %>;">
                                <%= dept.isAvailable ? 'القسم متاح' : 'القسم مغلق' %>
                            </span>
                            <div class="header-right">
                                <h2 class="department-title"><%= dept.name %></h2>
                                <span class="department-icon" style="background-color: <%= dept.color || '#ccc' %>;"><i class="fas fa-<%= dept.icon %>"></i></span>
                            </div>
                        </div>
                        <div class="progress-container">
                             <div class="progress-label">
                                <span>توفر الطاقم</span>
                                <span><%= dept.activeStaffCount %> / <%= dept.staff.length %></span>
                             </div>
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: <%= availability %>%; background-color: <%= dept.color || '#ccc' %>;"></div>
                            </div>
                        </div>
                        <div class="staff-container">
                            <h3 class="staff-title">الطاقم المناوب حاليًا</h3>
                             <% if(onDutyStaff.length > 0) { %>
                                <% onDutyStaff.forEach(member => { %>
                                    <div class="staff-item">
                                        <div class="staff-details">
                                            <div class="staff-name"><%= member.doctor.fullName %></div>
                                            <div class="staff-role"><%= member.roleInDepartment %></div>
                                        </div>
                                        <div class="staff-avatar" style="background-color: var(--success-color);">
                                            <i class="fas fa-user-md"></i>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <p class="no-data" style="font-size: 1rem; padding: 10px 0;">لا يوجد طاقم مناوب حاليًا.</p>
                            <% } %>
                        </div>
                        <!-- --- تمت إضافة هذا الجزء --- -->
                        <div class="card-actions">
                            <a href="/hospital-panel/departments/<%= dept._id %>" class="action-btn details-btn"><i class="fas fa-eye"></i> عرض التفاصيل</a>
                            <a href="#" class="action-btn edit-btn"><i class="fas fa-edit"></i> تعديل</a>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p class="no-data">لا توجد أقسام معرفة لهذا المستشفى بعد.</p>
            <% } %>
        </div>

         <a href="/hospital-panel/dashboard" class="back-link">
            <i class="fas fa-arrow-left" style="margin-right: 8px;"></i>
            العودة إلى لوحة التحكم
        </a>
    </div>
</body>
</html>
